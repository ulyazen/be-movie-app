# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
    
env:
  GOOGLE_CLIENT_ID: 315209859245-pnp48v117ub8l307aj0eqsiu3h5og8aj.apps.googleusercontent.com
  GOOGLE_CLIENT_SECRET: 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
  SERVER_DB_TEST: mongodb+srv://zen:test123@cluster0.p3dxp.mongodb.net/?retryWrites=true&w=majority
  
jobs:
  build:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: borales/actions-yarn@v3.0.0
        with:
          cmd: install # will run `yarn install` command
      - uses: borales/actions-yarn@v3.0.0
        with:
          cmd: test # will run `yarn test` command
